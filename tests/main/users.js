const { expect } = require("chai")

const { client, data } = require("..")

describe("Users", () => {
    let u

    // it("should have the correct user ID", async () => {
    //     u = await client.users.get(data.testUser)
    //     expect(u.id).to.equal(data.testUser)
    // })
})
