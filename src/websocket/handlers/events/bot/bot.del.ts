import type { APIGroupMessage, BotDelEvent } from 'groupme-api-types'
import type { Client } from '../../../..'

export async function del(client: Client, message: APIGroupMessage, event: BotDelEvent) {
    return
}
