import type { APIGroupMessage, BotAddEvent } from 'groupme-api-types'
import type { Client } from '../../../..'

export async function add(client: Client, message: APIGroupMessage, event: BotAddEvent) {
    return
}
