import type { APIGroupMessage, GroupOfficeModeEnabledEvent } from 'groupme-api-types'
import type { Client } from '../../../..'

export async function officeModeEnabled(client: Client, message: APIGroupMessage, event: GroupOfficeModeEnabledEvent) {
    return
}
