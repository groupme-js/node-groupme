import type { APIGroupMessage, GroupRoleChangeAdminEvent } from 'groupme-api-types'
import type { Client } from '../../../..'

export async function roleChangeAdmin(client: Client, message: APIGroupMessage, event: GroupRoleChangeAdminEvent) {
    return
}
