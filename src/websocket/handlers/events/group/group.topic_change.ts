import type { APIGroupMessage, GroupTopicChangeEvent } from 'groupme-api-types'
import type { Client } from '../../../..'

export async function topicChange(client: Client, message: APIGroupMessage, event: GroupTopicChangeEvent) {
    return
}
