import type { APIGroupMessage, GroupAvatarChangeEvent } from 'groupme-api-types'
import type { Client } from '../../../..'

export async function avatarChange(client: Client, message: APIGroupMessage, event: GroupAvatarChangeEvent) {
    return
}
