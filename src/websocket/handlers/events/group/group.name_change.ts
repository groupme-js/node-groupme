import type { APIGroupMessage, GroupNameChangeEvent } from 'groupme-api-types'
import type { Client } from '../../../..'

export async function nameChange(client: Client, message: APIGroupMessage, event: GroupNameChangeEvent) {
    return
}
