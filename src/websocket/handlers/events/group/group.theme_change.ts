import type { APIGroupMessage, GroupThemeChangeEvent } from 'groupme-api-types'
import type { Client } from '../../../..'

export async function themeChange(client: Client, message: APIGroupMessage, event: GroupThemeChangeEvent) {
    return
}
