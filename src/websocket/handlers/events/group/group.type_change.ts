import type { APIGroupMessage, GroupTypeChangeEvent } from 'groupme-api-types'
import type { Client } from '../../../..'

export async function typeChange(client: Client, message: APIGroupMessage, event: GroupTypeChangeEvent) {
    return
}
