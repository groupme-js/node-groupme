import type { APIGroupMessage, PollReminderEvent } from 'groupme-api-types'
import type { Client } from '../../../..'

export async function reminder(client: Client, message: APIGroupMessage, event: PollReminderEvent) {
    return
}
