import type { APIGroupMessage, CalendarEventUserGoingEvent } from 'groupme-api-types'
import type { Client } from '../../../..'

export async function userGoing(client: Client, message: APIGroupMessage, event: CalendarEventUserGoingEvent) {
    return
}
